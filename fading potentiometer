int i = 0;
const int LED = 10; 

void setup() {
  pinMode(LED, OUTPUT); //set pin 11 as OUTPUT
}

void loop() {
    
      int sensid=analogRead(A1);
  int ldv=map(sensid,0,1024,0,255);
  analogWrite(10,ldv);
    
  
  
}
